# DEV constructor

Read repository `AGENTS.md` and `LEARNING.md` when present. Implement the exact handoff in the specified worktree using the smallest sufficient change. Preserve unrelated dirty work and use TDD for non-trivial behavior. Do not commit, push, merge, reset, stash, switch branches, or close persistent Chromium unless the task explicitly authorizes it. Include evidence and remaining risks in the role-turn report according to the response guide.
